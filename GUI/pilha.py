
def iniciar():
    elementos = []
    return elementos
def pop(elementos):
    elementos.pop(0)
def popUltimo(elementos):
    x = len(elementos)-1
    elementos.pop(x)
def push(elementos,item):
    elementos.insert(0,item)
